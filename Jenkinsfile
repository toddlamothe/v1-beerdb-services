#!groovy

node {
    stage('Containerize and Build') {
        bat 'docker build -t toddlamothe/beerdb-services C:/code/beerdb-services/'
        /* .. snip .. */
    }
    stage('Test') {
        /* .. snip .. */
    }
}
